{% extends 'base.html.twig' %}

{% block title %}Edit city{% endblock %}
{% block pageTitle %}Edit city{% endblock %}

{% block body %}

    <div class="p-3">
        {{ include('cities/_form.html.twig', {'button_label': 'Update'}) }}

        <a class="text-decoration-none" href="{{ path('app_cities_index') }}">
            <button class="btn btn-sm btn-outline-dark ms-2 my-2">Back to list</button>
        </a>

        {{ include('cities/_delete_form.html.twig') }}
    </div>

{% endblock %}
