<form method="post" action="{{ path('app_cities_delete', {'id': city.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ city.id) }}">
    <button class="btn btn-sm btn-outline-danger ms-2">Delete</button>
</form>
